/**
 * Server Actions barrel file.
 * Export all server actions from here.
 *
 * Server Actions enable secure, type-safe server-side mutations
 * that can be called directly from Client Components.
 *
 * @example
 * // Define an action
 * export async function createSite(formData: FormData) {
 *   "use server";
 *   // ... implementation
 * }
 *
 * // Use in a component
 * import { createSite } from "@/actions";
 * <form action={createSite}>...</form>
 */

// Placeholder - add actions as features require them
// export { createSite, updateSite, deleteSite } from "./site";
// export { updateSettings } from "./settings";
// export { login, logout } from "./auth";
